{
  "name": "hello-pdf-lambda",
  "version": "1.0.0",
  "description": "PDF generator in AWS lambda",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "dist:zip": "zip -r target/lambda index.js node_modules",
    "dist": "rm -rf node_modules && yarn install --production && mkdir -p target && yarn run dist:zip"
  },
  "dependencies": {
    "chrome-aws-lambda": "^2.1.1",
    "middy": "^0.34.0",
    "puppeteer-core": "^2.1.1",
    "tmp": "^0.1.0"
  },
  "devDependencies": {
    "puppeteer": "^2.1.1"
  }
}
